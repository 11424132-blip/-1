# 學號：11424132 姓名：蕭梓敬

# 📝 題目

求下列函數的傅立葉級數（Fourier Series）：
$$f(x)=x+\pi,\qquad -\pi\le x\le \pi$$

---

# 🛠️ 步驟 1：傅立葉級數公式

週期為 $2\pi$ 的傅立葉級數展開式為：
$$f(x) = \frac{a_0}{2} + \sum_{n=1}^{\infty} \left( a_n\cos nx+b_n\sin nx \right)$$

其中各係數定義如下：
$$a_0 = \frac{1}{\pi} \int_{-\pi}^{\pi} f(x)\,dx$$

$$a_n = \frac{1}{\pi} \int_{-\pi}^{\pi} f(x)\cos(nx)\,dx$$

$$b_n = \frac{1}{\pi} \int_{-\pi}^{\pi} f(x)\sin(nx)\,dx$$

---

# 🔢 步驟 2：求常數項 $a_0$

代入 $f(x)=x+\pi$：
$$a_0 = \frac{1}{\pi} \int_{-\pi}^{\pi} (x+\pi)\,dx$$

$$= \frac{1}{\pi} \left[ \frac{x^2}{2} +\pi x \right]_{-\pi}^{\pi}$$

$$= \frac{1}{\pi} \left( 2\pi^2 \right) = 2\pi$$

因此：
$$\frac{a_0}{2} = \pi$$

---

# 📐 步驟 3：求餘弦係數 $a_n$

$$a_n = \frac{1}{\pi} \int_{-\pi}^{\pi} (x+\pi)\cos(nx)\,dx$$

拆解為兩個積分：
$$a_n = \frac{1}{\pi} \int_{-\pi}^{\pi} x\cos(nx)\,dx + \frac{1}{\pi} \int_{-\pi}^{\pi} \pi\cos(nx)\,dx$$

> ### 💡 奇偶函數性質分析
> * $x$ 為 **奇函數 (Odd Function)**
> * $\cos(nx)$ 為 **偶函數 (Even Function)**
> 
> 因此，乘積 $x\cos(nx)$ 為 **奇函數**。

根據奇函數在對稱區間的積分性質：
$$\int_{-\pi}^{\pi} x\cos(nx)\,dx = 0$$

### 🔔 第二部分積分
對於正整數 $n$，餘弦函數在一個完整週期內的積分為零：
$$\int_{-\pi}^{\pi} \cos(nx)\,dx = 0$$

綜合以上結果：
$$a_n=0$$

---

# 🌊 步驟 4：求正弦係數 $b_n$

$$b_n = \frac{1}{\pi} \int_{-\pi}^{\pi} (x+\pi)\sin(nx)\,dx$$

拆解為兩個積分：
$$b_n = \frac{1}{\pi} \int_{-\pi}^{\pi} x\sin(nx)\,dx + \frac{1}{\pi} \int_{-\pi}^{\pi} \pi\sin(nx)\,dx$$

### 🔔 第二部分積分
正弦函數在對稱區間上的積分為零：
$$\int_{-\pi}^{\pi} \sin(nx)\,dx = 0$$

因此，係數簡化為：
$$b_n = \frac{1}{\pi} \int_{-\pi}^{\pi} x\sin(nx)\,dx$$

### 💡 利用偶函數性質簡化
因為：
* $x$ 為 **奇函數**
* $\sin(nx)$ 為 **奇函數**

兩個奇函數相乘，其乘積 $x\sin(nx)$ 為 **偶函數**。利用對稱性可將積分區間減半：
$$b_n = \frac{2}{\pi} \int_0^{\pi} x\sin(nx)\,dx$$

### 🧮 分部積分法 (Integration by Parts)
令：
$$u=x, \qquad dv=\sin(nx)\,dx$$

則：
$$du=dx, \qquad v=-\frac{\cos(nx)}{n}$$

應用分部積分公式 $\int u\,dv = uv-\int v\,du$：
$$\int x\sin(nx)\,dx = -\frac{x\cos(nx)}{n} + \frac{\sin(nx)}{n^2}$$

代入積分上下限：
$$\int_0^\pi x\sin(nx)\,dx = \left[ -\frac{x\cos(nx)}{n} + \frac{\sin(nx)}{n^2} \right]_0^\pi$$
$$= -\frac{\pi\cos(n\pi)}{n}$$

因為：
$$\cos(n\pi) = (-1)^n$$

所以：
$$\int_0^\pi x\sin(nx)\,dx = -\frac{\pi(-1)^n}{n}$$

因此：
$$b_n = \frac{2}{\pi} \left( -\frac{\pi(-1)^n}{n} \right) = \frac{2(-1)^{n+1}}{n}$$

---

# 🎼 步驟 5：代入傅立葉級數通式

已知各項參數為：
* $a_0=2\pi$
* $a_n=0$
* $b_n = \frac{2(-1)^{n+1}}{n}$

代入傅立葉級數公式中：
$$f(x) = \frac{a_0}{2} + \sum_{n=1}^{\infty} (a_n\cos nx+b_n\sin nx)$$

我們得到：
$$f(x) = \pi + \sum_{n=1}^{\infty} \frac{2(-1)^{n+1}}{n} \sin(nx)$$

---

# 🏆 最終答案

**級數和形式：**
$$\boxed{ x+\pi = \pi + 2 \sum_{n=1}^{\infty} \frac{(-1)^{n+1}}{n} \sin(nx) }$$

**展開式形式：**
$$\boxed{ x+\pi = \pi + 2 \left( \sin x -\frac{\sin 2x}{2} +\frac{\sin 3x}{3} -\frac{\sin 4x}{4} +\cdots \right) }$$
