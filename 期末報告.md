import numpy as np
import matplotlib.pyplot as plt

# 電路參數
R = 10       # ohm
L = 10e-3    # H
C = 100e-6   # F

# 直流電源
Vs = 12

# 初始條件
V0 = 0       # capacitor voltage
I0 = 0       # inductor current

# 計算參數
alpha = R / (2 * L)
wn = 1 / np.sqrt(L * C)

wd = np.sqrt(wn**2 - alpha**2)

A = V0 - Vs
B = (I0 / C + alpha * A) / wd

# 時間軸
t = np.linspace(0, 0.02, 2000)

# 欠阻尼響應
vc = Vs + np.exp(-alpha * t) * (
        A * np.cos(wd * t)
      + B * np.sin(wd * t)
)

# 繪圖
plt.plot(t, vc)
plt.xlabel("Time (s)")
plt.ylabel("Vc(t)")
plt.title("Series RLC Transient Response")
plt.grid(True)
plt.show()
