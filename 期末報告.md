# 簡單三步驟：求解 RLC 串聯電路暫態回應

當 $t = 0$ 接入直流電源 $V_s$ 時，求電容電壓 $v_c(t)$ 的步驟如下：

---

## 步驟一：計算兩個核心參數
先用你的電路數值（$R, L, C$），算出以下兩個數字：
1. **衰減係數** $\alpha = \frac{R}{2L}$
2. **諧振角頻率** $\omega_0 = \frac{1}{\sqrt{LC}}$

---

## 步驟二：判斷電路狀態（三選一）
比較 $\alpha$ 和 $\omega_0$ 的大小，決定你的電容電壓公式：

### 1. 欠阻尼狀態（當 $\alpha < \omega_0$）—— *最常見的濾波器狀態*
電壓會像波浪一樣**振盪縮小**，最後穩定在 $V_s$。
* **公式：** $v_c(t) = V_s + e^{-\alpha t} [B_1 \cos(\omega_d t) + B_2 \sin(\omega_d t)]$
* *其中 $\omega_d = \sqrt{\omega_0^2 - \alpha^2}$*

### 2. 過阻尼狀態（當 $\alpha > \omega_0$）
電壓**完全不振盪**，像爬坡一樣慢慢升到 $V_s$。
* **公式：** $v_c(t) = V_s + A_1 e^{\lambda_1 t} + A_2 e^{\lambda_2 t}$
* *其中 $\lambda_{1,2} = - \alpha \pm \sqrt{\alpha^2 - \omega_0^2}$*

### 3. 臨界阻尼狀態（當 $\alpha = \omega_0$）
電壓**不振盪，且用最快的速度**升到 $V_s$。
* **公式：** $v_c(t) = V_s + (D_1 + D_2 t) e^{-\alpha t}$

---

## 步驟三：代入初始條件求係數
把題目給的初始值代入公式，求出裡面的未知數（如 $B_1, B_2$ 或 $A_1, A_2$）：
1.  **時間 $t = 0$ 時的電容電壓：** $v_c(0) = \text{題目給的電容初始電壓}$
2.  **時間 $t = 0$ 時的電壓變化率：** $\frac{dv_c(0)}{dt} = \frac{\text{題目給的電感初始電流}}{C}$

將這兩個條件聯立，就能解出最終的唯一答案！
